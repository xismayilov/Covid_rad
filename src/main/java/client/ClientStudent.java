package client;

public class ClientStudent extends Client {
    public ClientStudent(String email, int ID, String password, String name, String surname) {
        super(email, ID, password, name, surname);
    }
}
