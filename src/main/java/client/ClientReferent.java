package client;

public class ClientReferent extends Client {
    public ClientReferent(String email, int ID, String password, String name, String surname) {
        super(email, ID, password, name, surname);
    }
}
